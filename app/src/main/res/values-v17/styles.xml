<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="EventText">
        <!-- Customize your theme here. -->
        <item name="android:layout_marginRight">@dimen/padding_horizontal</item>
        <item name="android:layout_marginEnd">@dimen/padding_horizontal</item>
        <item name="android:layout_marginStart">@dimen/event_text_padding_left</item>
        <item name="android:layout_marginLeft">@dimen/event_text_padding_left</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>
</resources>