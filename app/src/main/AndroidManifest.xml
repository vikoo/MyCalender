<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vikoo.calenderapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".CalenderApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.calenderview.CalenderActivity"
            android:screenOrientation="portrait"
            android:label="@string/title_calender">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:screenOrientation="portrait"
            android:label="@string/title_agenda"
            android:name=".screens.agendaview.AgendaActivity"
            android:parentActivityName=".screens.calenderview.CalenderActivity" />

    </application>

</manifest>